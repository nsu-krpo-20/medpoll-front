import { createSignal } from 'solid-js'
import './TopBar.css'

function TopBar() {
  return ( <>
      <header class="topbar h-full">
		<h1> MedPoll </h1>
	  </header>
    </> )
}

export default TopBar;
